// === app/src/test/java/com/blackcloud/shell/ExampleUnitTest.kt ===
package com.blackcloud.shell

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Temel bir birim (unit) testi. Temel mantıksal yapıları ve paket bütünlüğünü doğrular.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
